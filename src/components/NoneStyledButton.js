import styled from 'styled-components';

const NoneStyledButton = styled.button`
  color: var(--text-light);
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
  font-size: var(--font-size);
  margin-left: 1em;
  letter-spacing: 1px;
  text-transform: uppercase;
`;

export default NoneStyledButton;
